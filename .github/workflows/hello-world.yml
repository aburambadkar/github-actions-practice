name: hello

on:
 push:
   branches: [main]


jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
    - name: Checking repo
      uses: actions/checkout@v4
    - name: Print message
      run: echo "Hello from GitHub Actions!"  
